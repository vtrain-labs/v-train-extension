# 更新日誌 (Changelog)

## [1.0.6] - 2026-06-17

### Added (新增)
- **免費版開放書籤功能 (Free Version Bookmark Features)**：正式全面開放「本機書籤庫」功能給免費版使用者（書籤上限 100 部）。
- **無限紀錄架構突破 (Infinite Records for Pro)**：受惠於底層 IndexedDB 引擎的效能突破，全面移除了過去 Pro 版的 20 萬筆紀錄上限限制，現在已達成物理層面上的真正「無上限」紀錄追蹤。

### Fixed (修復)
- **免費版配額聯集計算 (Unified Quota Calculation)**：重構了免費版的額度計算邏輯（總額度 = 書籤 + 歷史紀錄 = 200 部）。現在書籤會如預期地佔用整體配額，當額度全滿時，行車紀錄器將優先並精準地汰換掉最舊的一般歷史紀錄，確保珍貴的收藏絕不遺失。
- **配額動態即時同步 (Dynamic Quota Sync)**：補齊了跨分頁與管理後台的廣播機制。現在無論是在播放頁面點擊收藏，還是在 Bookmark Vault 內部刪除書籤，總配額與介面進度條都會在毫秒級別內跨視窗即時刷新。
- **清除資料重置邏輯 (Clear Data Calculation)**：修復了在控制面板點擊「清除資料」時，雖然書籤被成功保留，但畫面上的追蹤數字卻被錯誤歸零的 Bug。現在系統會精準結算殘留的書籤並顯示正確的佔用額度。

## [1.0.7] - 2026-06-20

### Added (新增)
- **內建 YouTube & Bilibili 專屬追蹤引擎 (Built-in Global Tracking Engine)**：現在 V-Train 出廠時將自動在背景注入並啟用針對 YouTube 與 Bilibili (B站) 的最佳化預設解析規則。用戶不需手動匯入設定，安裝後即可享受開箱即用的兩大平台精準進度追蹤與縮圖抓取功能。
- **專屬進度條顏色功能 (Exclusive Progress Bar Color)**：現在可以為「普通」、「喜歡」、「不喜歡」三種不同的影片評價狀態，分別設定專屬的進度條顏色，並且會在網頁畫面上即時切換套用。
- **第二道截圖防線 (Fallback Snapshot via Context Menu)**：針對部分影片網站（如跨域廣告干擾、CORS 限制）導致點擊 📸 截圖失敗的問題，加入了自動偵測與「動態右鍵變身」機制。若一鍵截圖失敗，只需對網頁按右鍵並點擊「強制物理截圖」，即可利用 `activeTab` 權限穿透限制，確保截圖 100% 成功。

### Changed (變更)
- **通用化分享序號 (Generic Sharing Code)**：為了提升工具的中立性與社群分享的隱私，將匯出的設定檔序號前綴從 `VT-RULE-` 更改為通用的 `SYNC-`。舊版 `VT-RULE-` 序號依然可以無縫匯入且完全相容。

### Fixed (修復)
- **YouTube ID 大小寫與 404 縮圖問題 (YouTube Case-Sensitivity & 404 Thumbnail Fix)**：徹底移除了早期針對 YouTube 影片 ID 的「強制小寫」舊規則。這確保了系統能使用 100% 正確大小寫的 ID 向 YouTube 官方 API 請求高畫質縮圖，完美解決了圖片抓不到 (404) 的問題；同時也修復了因主播放器與側邊欄 ID 大小寫比對失敗，導致的「愛心不同步」視覺 Bug。
- **全螢幕面板遮擋修復 (Fullscreen Panel Interference)**：加入了 `fullscreenchange` 系統監聽機制。現在當您在觀看影片並進入全螢幕模式時，右下角的書籤互動面板會自動隱身，退出全螢幕時自動恢復，給您毫無遮擋的純淨觀影體驗。
- **英文語系 Modal 文字顯示 (English i18n Fallback)**：修復了在純英文語系下，控制面板與救援模式 Modal 的變數名稱（如 `modalRescue`）因為缺少翻譯檔而直接顯示原始碼的介面錯誤。


## [1.0.5] - 2026-06-15

### Added (新增)
- **全新獨立備份與還原中心 (ZIP Backup Center)**：打造了全螢幕、沉浸式的獨立備份控制面板。完全支援八國語言即時切換。
- **極限壓縮與資料分流引擎 (Data Separation Engine)**：改變了過去單一 JSON 備份檔容易肥大當機的缺點。現在系統會將「核心設定與書籤 (core_data.json)」與「二進位圖片庫 (images_part_X.json)」進行智能分離。並支援最高 10 萬張圖片的記憶體分塊打包壓縮，確保在極端資料量下依然滑順不崩潰。

### Fixed (修復)
- **CORS 跨域阻擋錯誤修復**：將圖片拉取邏輯由擴充功能後台 (Background) 轉移至網頁前台 (Content Script)，徹底解決了在收藏書籤時，因網站跨域策略導致在擴充功能管理頁面 (`chrome://extensions`) 產生大量紅字報錯的困擾。
- **舊版 Base64 遺毒自動淨化器 (Legacy Data Auto-Migration)**：實作了針對舊版系統架構的自動清洗引擎。當使用者匯入舊版備份或進行資料匯出時，系統會自動將書籤內的肥大 Base64 圖片編碼抽離，並轉移至全新的 `vt_thumbnails` 圖片庫，永久最佳化使用者的本地資料庫效能。

## [1.0.4] - 2026-06-14

### Added (新增)
- **書籤庫 - 移動資料夾功能 (Move Folder)**：在本機書籤庫（`bookmarks.html`）的資料夾右鍵選單中，新增了「移動資料夾」功能。支援將整個資料夾（包含其內部的影片書籤與子資料夾）完整搬移到另一個資料夾底下，並完整支援八國語言包介面顯示。

### Fixed (修復)
- **影片縮圖精準度優化**：修復了在部分影音網站點擊收藏時，會錯誤抓取到側邊欄推薦影片縮圖的問題。現已優化腳本抓取權重，優先解析網頁的 `schema.org` 與 `og:image` 中繼資料，確保能精準捕捉當前播放主影片的預覽圖。

## [1.0.3] - 2026-06-14

### Fixed (修復)
- **跨域影片截圖 (Cross-Origin Snapshot)**：深度重構截圖系統，實作「跨層級截圖指令接力賽 (Cross-Frame Message Relay)」。外部面板的截圖指令現在能精準穿透多層 iframe，直達最深處的影片原始環境進行原生 Canvas 截圖，完美繞過 Chrome 硬體加速導致的黑屏限制。
- **防止假影片誤判**：增強截圖判定邏輯，透過 `sysState._activeEl` 確保只對正在追蹤的影片進行截圖，不再被網頁最上層的隱藏廣告或 dummy video 標籤干擾而顯示截圖失敗 (紅X)。
- **清理重複面板**：修復了當影片被包裝在 iframe 內時，會產生兩個浮動控制面板（外部一個、影片內部一個）的 UI 重疊錯誤。

## [1.0.2] - 2026-06-14

### Fixed (修復)
- **嚴重錯誤 (Critical Bug)**：修復了當影片播放時，系統自動儲存進度會覆蓋並清空使用者自訂的「收藏縮圖 (Thumbnail)」、「標題」與「書籤狀態」的錯誤。現在資料庫寫入已改為「局部合併 (Partial Update)」，完美保留使用者的客製化封面與評分狀態。
- 移除了未完善的跨域 Iframe 實驗性解鎖功能，維持主介面乾淨與擴充功能的穩定性。

## [1.0.1] - 2026-06-10

### Added (新增)
- 首次發布至 Chrome 線上應用程式商店。
- 支援 PRO 版「書籤管理中心」與「隱私密碼鎖定」。
- 支援各大影音網站的進度條覆蓋與追蹤。
